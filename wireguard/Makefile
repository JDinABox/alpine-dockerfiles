# Auto generated by github.com/jdinabox/alpine-dockerfiles

build:
	go build -ldflags="-s -w" -o ./cmd/app.so ./cmd

mkfile_path := $(abspath $(lastword $(MAKEFILE_LIST)))
mkfile_dir := $(dir $(mkfile_path))
root_dir :=  $(realpath $(mkfile_dir)../)

docker-build:
	# docker build --tag alpine-wireguard:latest . 
	cd $(root_dir); docker build -f wireguard.Dockerfile --tag alpine-wireguard:latest . 